meta {
  name: List Users As Student Forbidden
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/users
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("status 403", function() {
    expect(res.getStatus()).to.equal(403);
  });
}