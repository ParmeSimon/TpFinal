meta {
  name: List All Bookings As Student Forbidden
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/bookings
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("status 403", function() {
    expect(res.getStatus()).to.equal(403);
  });
}