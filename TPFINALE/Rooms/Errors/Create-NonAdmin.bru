meta {
  name: Create Room As Student Forbidden
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/rooms
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": "Forbidden Room",
    "capacity": 5,
    "description": "should fail",
    "available": true,
    "imageUrl": null,
    "equipmentIds": []
  }
}

tests {
  test("status 403", function() {
    expect(res.getStatus()).to.equal(403);
  });
}