meta {
  name: Delete Room As Student Forbidden
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/api/rooms/{{roomId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("status 403", function() {
    expect(res.getStatus()).to.equal(403);
  });
}