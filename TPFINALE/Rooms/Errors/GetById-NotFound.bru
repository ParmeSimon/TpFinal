meta {
  name: Get Room Not Found
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/rooms/999999
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("status 404", function() {
    expect(res.getStatus()).to.equal(404);
  });
}