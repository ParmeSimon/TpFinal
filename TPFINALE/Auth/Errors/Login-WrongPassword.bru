meta {
  name: Login Wrong Password
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "{{studentEmail}}",
    "password": "WrongPass99999"
  }
}

tests {
  test("status 401", function() {
    expect(res.getStatus()).to.equal(401);
  });
}